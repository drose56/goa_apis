// Code generated by goa v3.7.6, DO NOT EDIT.
//
// new_poke gRPC client CLI support package
//
// Command:
// $ goa gen new_poke/design

package cli

import (
	"flag"
	"fmt"
	newpokec "new_poke/gen/grpc/new_poke/client"
	"os"

	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `new-poke pokemon
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` new-poke pokemon --message '{
      "name": "Quae quam exercitationem consectetur provident ut illo."
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, interface{}, error) {
	var (
		newPokeFlags = flag.NewFlagSet("new-poke", flag.ContinueOnError)

		newPokePokemonFlags       = flag.NewFlagSet("pokemon", flag.ExitOnError)
		newPokePokemonMessageFlag = newPokePokemonFlags.String("message", "", "")
	)
	newPokeFlags.Usage = newPokeUsage
	newPokePokemonFlags.Usage = newPokePokemonUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "new-poke":
			svcf = newPokeFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "new-poke":
			switch epn {
			case "pokemon":
				epf = newPokePokemonFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "new-poke":
			c := newpokec.NewClient(cc, opts...)
			switch epn {
			case "pokemon":
				endpoint = c.Pokemon()
				data, err = newpokec.BuildPokemonPayload(*newPokePokemonMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// new-pokeUsage displays the usage of the new-poke command and its subcommands.
func newPokeUsage() {
	fmt.Fprintf(os.Stderr, `return pokemon with same first letter as name
Usage:
    %[1]s [globalflags] new-poke COMMAND [flags]

COMMAND:
    pokemon: Pokemon implements pokemon.

Additional help:
    %[1]s new-poke COMMAND --help
`, os.Args[0])
}
func newPokePokemonUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] new-poke pokemon -message JSON

Pokemon implements pokemon.
    -message JSON: 

Example:
    %[1]s new-poke pokemon --message '{
      "name": "Quae quam exercitationem consectetur provident ut illo."
   }'
`, os.Args[0])
}
